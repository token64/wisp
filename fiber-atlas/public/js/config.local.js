/**
 * Fiber Atlas — claves en el servidor (opcional).
 * Si no descomenta nada aquí, use el panel «Google Maps — clave API oficial» → Guardar (localStorage).
 *
 * No suba claves reales a repositorios públicos.
 */
(function (w) {
  if (typeof w === "undefined" || !w) return;
  // w.FIBER_ATLAS_GOOGLE_MAPS_KEY = w.FIBER_ATLAS_GOOGLE_MAPS_KEY || "PEGAR_CLAVE_AQUI";
})(typeof window !== "undefined" ? window : undefined);
